language: python
python:
  - "2.7"
env:
  - DJANGO=1.4
install:
  - pip install -qr requirements.txt --use-mirrors
  - pip install -q Django==$DJANGO --use-mirrors
  - pip install -q PIL --use-mirrors
  - pip install -q simplejson --use-mirrors
  - pip install -q factory_boy --use-mirrors
  - pip install -q mock --use-mirrors
  - pip install -q . --use-mirrors
script:
  - python quicktest.py vkontakte_ads
notifications:
  email:
    recipients:
      - ramusus@gmail.com
    on_success: change
    on_failure: change